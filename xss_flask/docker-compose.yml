version: "3"

services:

  # Web service
  xss:
    build: ./
    image: xss-flask
    # image: tiangolo/uwsgi-nginx:python3.8-alpine
    container_name: xss-flask
    hostname: web-flask
    ports:
        - "5000:5000"
        - "5001:5001"
    restart: unless-stopped
    # volumes:
    #     - ./app:/app
